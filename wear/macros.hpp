
#include <stringapiset.h>
#include <string>

namespace wear {
	using namespace std;
	typedef unsigned char byte;

	struct uniString {
		string convert(string text) {
			string data = L"" + text;
		}
	};
}
